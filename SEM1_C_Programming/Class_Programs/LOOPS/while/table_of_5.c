//Q.4 Table of 5
#include <stdio.h>
int main()
{
    int count=5;
    while(count<=50) // while(i<6)
    {
   printf("\n %d",count);
    count= count+5; // count++; or count+=1;
    }
    return 0;
}