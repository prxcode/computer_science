//Even number (option 2)
#include <stdio.h>
int main()
{
    int count=0;
    while(count<=30) // while(i<6)
    {
   printf("\n %d",count);
    count= count+2; // count++; or count+=1;
    }
    return 0;
}