//Q.2 print first 10 natural number
#include <stdio.h>
int main()
{
    int count=1;
    while(count<=10) // while(i<6)
    {
   printf("\n %d",count);
    count= count+1; // count++; or count+=1;
    }
    return 0;